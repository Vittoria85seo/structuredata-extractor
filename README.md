# Structured Data Extractor

Streamlit app that extracts JSON-LD, Microdata, and RDFa from a list of URLs
and exports the results to an Excel file.

## How to run

pip install -r requirements.txt
streamlit run app.py
